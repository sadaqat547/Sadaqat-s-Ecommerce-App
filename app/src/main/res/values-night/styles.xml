<resources>
    <!-- Base application theme. -->
    <style name="AppThemenight" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimarynight</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarknight</item>
        <item name="colorAccent">@color/colorAccentnight</item>
    </style>

    <style name="SplashThemenight" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:windowBackground">@drawable/splashactivitybackground</item>
    </style>

    <style name="ProgressbarredThemenight" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/progreesbarred</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/progreesbarred</item>
    </style>

    <style name="paytmThemenight" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/progreesbarred</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@android:color/holo_blue_dark</item>
    </style>

    <style name="AppTheme.NoActionBarnight">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="Theme.AppCompat" />

</resources>